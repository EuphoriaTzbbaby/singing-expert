# ===== MySQL 数据库连接 =====
# 注意：OSS 配置（AK/SK/Endpoint/Bucket）已经从 MySQL 的 config 表里按 key 读取，
#      不要再在 .env 里填 OSS 凭证（key=oss.endpoint / oss.accessKeyId / oss.accessKeySecret / oss.bucketName）
DATABASE_URL=mysql+pymysql://root:your_password@47.101.42.177:3306/singing_exper?charset=utf8mb4

# ===== CORS 允许的前端源（逗号分隔）=====
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:4173

# ===== 单文件上传上限 (字节) =====
MAX_PDF_SIZE=104857600
